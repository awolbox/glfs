# Check to see if a user configuration was provided
_has_user_config()
{ 
		if [ ! -z "$MAKESRC_USER_CONFIG" ];
		then
					source "$MAKESRC_USER_CONFIG"
					DEFAULT=0
					return 0
		else
					return 1
		fi
}

