# Make/Build source
makesrc() 
{ 
				
		[ $EUID -ne 0 ] && { exit $EX_NOPERM; }
				
		_has_keyring
		_read_SRCBUILD
	 	_has_needed_dependencies
		
		# cross-compilation toolchian

		# _source_tarball
}

