# Parse/Source SRCBUILD $vars
_read_SRCBUILD()
{
		# Sanity checks
		local parse=($@)
		
		for all in $(ls .)
		do
				: # If tooo many
		done

		if [ -f "./SRCBUILD" ] && [ -O "./SRCBUILD" ];
		then
				source ./SRCBUILD
		else
				_EX=$EXIT_UNAVAILABLE
				_die "invalid SRCBUILD"
		fi
}

