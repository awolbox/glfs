# Parse/Source SRCBUILD $vars
_read_SRCBUILD()
{
		# Sanity checks
		if [ -f "./SRCBUILD" ] && [ -O "./SRCBUILD" ];
		then
				source ./SRCBUILD
		else
				_EX=$EXIT_UNAVAILABLE
				_die "invalid SRCBUILD"
		fi
}

